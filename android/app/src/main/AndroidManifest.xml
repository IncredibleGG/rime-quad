<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        ⚠ 讀到這裡的人多半是來查「你說你不連網，為什麼有網路權限」的。答案：

        INTERNET 是「安裝時」權限，執行期無法取消。只要這個 app 帶著方案市集
        與應用內更新，權限清單上就一定看得到它 —— app 內的連網開關關掉也一樣
        看得到。我們不會宣稱自己沒有網路權限，那句話一查就穿幫。

        我們的作法是讓你查得到，共三層：
          1. 全 app 唯一的連網出口是 net/NetworkGate.kt。驗證方式是在
             android/app/src 底下 grep：URL( / HttpURLConnection /
             openConnection / Socket / OkHttp，結果必須只出現在那一個檔案。
             （XML 註解不能寫連續的減號，完整指令見 NetworkGate.kt 的檔頭。）
          2. 那個出口預設拒絕（NetworkGate.policy 的初值就是「關」），
             使用者沒有明確打開開關就一個連線都不會開。
          3. 每一次真的建立的連線都記進「連網」分頁的紀錄裡，可清除，
             且不含任何輸入內容。

        ACCESS_NETWORK_STATE 只用來讀「現在有沒有網路」（見
        NetworkGate.systemHasNetwork），不送出任何封包。
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
        應用內更新。本專案是側載發布(不經 Google Play),升級只能自己來。

        光有這個權限不夠:API 26 起「安裝未知的應用程式」是逐 app 的授權,
        使用者必須自己去系統設定打開,只能引導不能用對話框要
        (見 UpdateInstaller.openUnknownSourcesSettings)。
    -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!--
        ⚠ allowBackup=false。這一行是隱私邊界，不是效能設定，改它之前請先讀完。

        Android 的「自動備份」會把 app 私有目錄整個同步到使用者的 Google Drive。
        它是**系統元件代勞**的：不經過我們的行程，因此
          · 不經過 net/NetworkGate.kt，「單一出口」那句話管不到它；
          · 連網紀錄一筆都不會有 —— 使用者照我們教的方法查，會查到「乾淨」，
            但資料其實已經出境了。

        實測(bmgr backupnow + com.android.localtransport,見
        scripts/audit_offline.sh 檔頭)確認 allowBackup=true 時被打包帶走的有：
          · files/rime/user/*.userdb/    使用者詞庫 —— 打過什麼字的直接證據
          · files/rime/user/installation.yaml
                                        installation_id,一組跨重裝穩定的 UUID
          · files/rime/user/user.yaml    schema_access_time：何時用過哪個方案
          · files/rime/user/rimequad-store.json、sp/rimequad-store.xml
                                        裝了哪些第三方方案
          · files/net/                   連網紀錄本身

        為什麼是全關，而不是用 dataExtractionRules 精細排除詞庫：
          1. 就算規則寫得完美、只帶走佈景偏好，Google 端仍然會留下一筆
             「這個帳號裝了 org.rimequad.ime」。對一個以規避監控為賣點的
             輸入法來說，那正是我們在別處(User-Agent)努力避免的自報家門。
             換來的好處只是鍵盤高度倍率跟著換機走 —— 不成比例。
          2. 排除規則是 fail-open 的：只寫 <exclude> 時，將來新增的目錄
             預設會被備份。本專案其他每一道閘門都是 fail-closed,
             這裡不該破例。
          3. Android 11 以下走 android:fullBackupContent、12 以上走
             android:dataExtractionRules,兩套規則要各寫一份且都不能寫錯。
             寫錯的下場是靜默失效 —— 沒有任何錯誤訊息會告訴你詞庫還在外流。

        代價要說清楚：使用者換手機時，詞庫與設定不會自動跟過去。
        我們認為誠實的替代方案是**使用者自己匯出**(主動、看得見、
        去處由使用者決定),而不是背著他上傳到某家公司的雲端。
        ⚠ 匯出/匯入功能目前尚未實作，見 audit_offline.sh 檔頭的未竟事項。

        tools:replace 的用途：manifest 合併時，任何相依函式庫都可能帶進
        自己的 allowBackup。加上它，這裡的值一定贏，而不是看合併順序。
    -->
    <application
        android:name=".RimeApp"
        android:allowBackup="false"
        tools:replace="android:allowBackup"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.RimeQuad">

        <!-- 設定／診斷畫面，同時是 method.xml 的 settingsActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            設定畫面。軟鍵盤工具列的 settings action(規範 §8.6.6.1 的必備項)
            指向這裡,使用者按下齒輪就直接落在設定,不必先經過診斷頁。
            exported=false:沒有任何外部 app 需要開它。
        -->
        <activity
            android:name=".prefs.SettingsActivity"
            android:exported="false"
            android:label="@string/app_name" />

        <!--
            PackageInstaller 的狀態回報。

            必須是 manifest 宣告的靜態 receiver:安裝自己這個 app 的過程中,
            系統會把我們的行程收掉,動態註冊的 receiver 會一起消失,
            結果是永遠收不到成功或失敗的通知。
            exported=false:只有系統會用我們自己給出去的 PendingIntent 送進來。
        -->
        <receiver
            android:name=".update.UpdateInstallReceiver"
            android:exported="false" />

        <!-- 輸入法服務宣告 -->
        <service
            android:name=".RimeInputMethodService"
            android:exported="true"
            android:label="@string/ime_label"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

    </application>

</manifest>
